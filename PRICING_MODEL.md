# PreciCV / SpeCV — מודל תמחור ותשלומים

עדכון אחרון: 2026-07-25. מסמך זה מגדיר את **שיטת התמחור** ואת שכבת התשלומים,
וממפה אותן לקוד הקיים כדי שאפשר יהיה להטמיע ישירות. מחליף את הטיוטה הישנה
`IBI/pricing-spec.md`.

> **סטטוס:** שינויי הקוד (תמחור $3/$4, הסתרת `base`, נתיב שדרוג +$1, מעבר
> Stripe→Lemon Squeezy) **מיושמים בברנץ' `freemium-funnel-and-oauth` ונבדקו
> מקומית**. נותר: deploy ל-Vercel, פתיחת/השלמת חשבון Lemon Squeezy עם 3 variants,
> וחיבור המפתחות ב-env (ראה §5 + HANDOFF).

---

## 1. הקשר המוצר

PreciCV (מותג בפועל: **SpeCV**) הוא כלי AI שמייצר קורות חיים מותאמים לכל משרה +
דוח שינויים/פערים + דוח סימולציית ראיון. הזרימה: העלאת CV + הדבקת JD → שאלון →
**הרשמה כדי לראות תוצאה** → עמוד תוצאה עם CV + דוח **מטושטש וממותג בווטרמארק** →
כפתור "Unlock" → תשלום.

Stack: Next.js 16, Supabase (טבלאות `profiles`, `jobs`, `generations`,
`purchases` + RLS), מנוע AI Gemini/Claude. חיוב: **Lemon Squeezy** hosted
checkout + webhook (ראה §5).

---

## 2. אסטרטגיה — שלב נוכחי: טראפיק, לא רווח

המטרה בשלב זה **צמיחה ואיסוף משתמשים**, לא מקסום הכנסה. העלות התפעולית זניחה:
מדוח שימוש בפועל, ~2 אגורות לקריאת API בודדת, ~$0.06-0.11 לפלואו שלם. לכן:

- החינמי נדיב בכוונה — משתמש חינמי עולה ~₪1, זהו "פיתיון" זול שמוכיח ערך.
- הווטרמארק על ה-CV הסופי הוא **ערוץ הפצה** — כל CV שנשלח למעסיק נושא את המותג.
- הנכס האמיתי בשלב זה: **אימיילים** (שער ההרשמה) לצורך מונטיזציה עתידית.
- המונטיזציה קלה ואימפולסיבית ($3-4), בלי מנויים ובלי חיכוך.

---

## 3. שיטת התמחור שנקבעה (Target)

מטבע: USD. מודל: תשלום פר-רכישה (one-time unlock) לכל `job`. ללא מנוי בשלב זה.

**עדכון 2026-08-02 — מוצר אחד בלבד.** ה-tier `match` ($3, קו"ח בלי דוח
הסימולציה) ו-`base` ($2) בוטלו. כל רכישה כוללת את כל המסמכים.

| Tier (`TierId`) | מוצר | מחיר | כולל | דורש JD |
|---|---|---|---|---|
| `full` | Full Prep | **$4** | CV בסיס מעודכן + CV מותאם למשרה + דוח השוואה + דוח סימולציית ראיון | כן |

### למה בוטל הפיצול

ההבחנה בין "קו"ח בלבד" ל"קו"ח + דוחות" הייתה המקור היחיד לכל אלה: שני כפתורי
הורדה נפרדים, החלקים המטושטשים בדוח הסימולציה, שלושה מסלולי שדרוג נפרדים
(פר-משרה, פר-הזמנה, ו-`applyOrderUpgrade`), ופאיוול דו-כרטיסי בכל נקודת החלטה.
לבקש מהמשתמש לתמחר חבילת מסמכים לפני שראה מסמך אחד היא חיכוך ברגע הגרוע ביותר.
עם מוצר אחד — הכמות היא ההחלטה היחידה שנשארה.

**נתיב שדרוג:** אין. אין ממה לשדרג.

### היגיון התמחור

- $4 היה תמיד המחיר ה"אמיתי"; ה-$3 רק עיגן אותו. הסרת העוגן לא משנה את
  ההכנסה הצפויה פר-משרה, ומוחקת את כל מכונת השדרוגים.
- התמחור הוא **value-based**, לא cost-based: דוח הסימולציה עולה עוד כמה אגורות
  בלבד, אבל שווה יותר ללקוח (קרוב יותר לראיון) — ולכן הוא נכלל, לא נמכר בנפרד.

---

## 3א. תמחור כמות (חבילות קרדיטים) — מיושם

עדכון 2026-07-31, תוקן 2026-08-02. מודל חבילות אגרסיבי לחדירת שוק.
**מקור האמת בקוד: `src/lib/packs.ts`.**

| מס' משרות | מחיר | עוגן |
|---|---|---|
| 1 | $4 | — |
| 2 | $6 | ~~$8~~ |
| 3 | $8 | ~~$12~~ |
| 4 | $9 | ~~$16~~ |
| 5 | $10 | ~~$20~~ |

העוגנים נגזרים מ-`TIERS` (`priceCents × כמות`) ולא מקודדים ידנית, כך שהם לא
יכולים להתנתק מהמחיר הבודד. חמישה קו"ח ב-$10 הם $2 למשרה מול $4 ליחידה בודדת.

### מודל הזכאות: קרדיטים

- רכישת חבילה יוצרת שורה ב-`orders` (מיגרציה 0010) ומעניקה **קרדיטים**, לא גישה.
- מימוש קרדיט על משרה יוצר את שורת ה-`purchases` הרגילה — לכן **כל בדיקות
  הזכאות הקיימות ממשיכות לעבוד ללא שינוי**, ו-`unique (job_id)` נשאר כפי שהוא.
- הקרדיטים **נגזרים** (`credits_total` פחות מספר ה-`purchases` שמצביעים על
  ההזמנה), לא נשמרים כמונה — אותה גישה כמו המכסה היומית ב-`free-quota.ts`.
- מימוש הוא FIFO בין הזמנות, ואטומי (`spend_credit()` עם נעילת שורה), אחרת שתי
  לשוניות יכולות לממש את אותו קרדיט אחרון פעמיים.

### הקצאת קרדיטים כשקנו פחות ממספר המשרות (עדכון 2026-08-01)

הדף הראשי אוסף עד 5 משרות בזו אחר זו, ולכן מצב של "5 משרות, 4 קרדיטים" הוא
תרחיש רגיל ולא קצה. ב-`/run/[id]` כל משרה נעולה מקבלת תיבת סימון החסומה
ב-`credits.total`, שורת `N of M credits allocated`, וכפתור `Generate N reports`
שמממש רק על מה שסומן. **המשתמש בוחר על מה הכסף שלו הולך** — לא N הראשונות.
הבחירה נגזרת מהיתרה (בלי effect שמזריע state), כך שחזרה מ-Lemon Squeezy נוחתת
על ברירת מחדל נכונה. המשרות שלא נבחרו נשארות נעולות עם כפתור `Use 1 credit`
משלהן, כך שאפשר לחזור ולקנות עוד בלי להתחיל מחדש.

התצוגה של `BundlePaywall` מובילה את הדף כשעוד לא נקנה ולא נוצר כלום — זה הרגע
שאחרי השאלות — ויורדת מתחת לרשימה ברגע שמשהו כבר נוצר.

### השפעה על עמלת Lemon Squeezy

זה בדיוק ה"שיקול לעתיד" שנרשם ב-§5: העמלה הקבועה של $0.50 נפרסת על פני מספר
משרות. ביחידה בודדת ב-$4 העמלה היא ~17% מההכנסה; בחבילת 5 ב-$10 היא ~9%. יחד עם
זאת, ההכנסה נטו **פר-משרה** יורדת — זו בחירה מודעת של חדירת שוק, לא שיפור
רווחיות.

---

## 4. מצב הקוד מול המודל — מיושם

אין פער. `TIERS` ב-`src/lib/types.ts` מחזיק מוצר יחיד (`full`, $4), ו-
`PACK_PRICE_CENTS` ב-`src/lib/packs.ts` מחזיק את טבלת הכמויות. כל מחיר שמוצג
נגזר משני אלה — אין מחרוזת מחיר מקודדת ב-UI.

מה נמחק עם ה-tier השני (2026-08-02):

1. **`match` ו-`base`** מ-`TIERS`, יחד עם `UPGRADE_ANCHOR_USD` ו-`VISIBLE_TIERS`.
2. **ציר ה-tier ב-`packs.ts`** — `PackTier`, `PACK_UPGRADE_CENTS`,
   `upgradeSku*`, `parsePackSku`. נשארה טבלה חד-ממדית לפי כמות.
3. **`order_upgrade`** — הסכימה, `orderUpgradeCheckout`, `applyOrderUpgrade`,
   `startOrderUpgradeCheckout`, וענף השדרוג ב-webhook.
4. **`byTier` / `nextTier` / `upgradeCents`** מ-`CreditBalance` — קרדיט הוא
   קרדיט.
5. **הפיצול בהורדה** — `simLocked` הצטמצם ל-`isSample` בלבד, `printFile("cv")`
   הוחלף ב-`printBoth` בכל מקום, ומסך ההיסטוריה חזר לכפתור אחד.
6. **משתני סביבה** — נותר `LEMONSQUEEZY_VARIANT_FULL` בלבד (ו-`_X2..X5`
   כאופציונליים). אין צורך בשינוי בדשבורד: `full_x1` הוא הווריאנט הקיים ב-$4,
   וכמויות 2–5 רוכבות עליו עם `custom_price`.

**מיגרציית נתונים:** `supabase/migrations/0011_single_tier.sql` מעביר כל
`purchases`/`orders` מ-`match`/`base` ל-`full`. זה חינם — הסימולציה תמיד נוצרה
ונשמרה בכל tier ורק הוצגה מטושטשת, ולכן קונים ותיקים מקבלים את הדוחות בלי אף
קריאת LLM.

---

## 5. ספק תשלומים — מעבר מ-Stripe ל-Lemon Squeezy (קריטי)

**חסם:** Stripe אינו זמין בישראל לפתיחת חשבון (46 מדינות נתמכות, ישראל לא ביניהן).
שכבת החיוב **הייתה** בנויה על Stripe ולכן לא הייתה ניתנת להפעלה בפרודקשן.

**החלטה:** לעבור ל-**Lemon Squeezy** כ-Merchant of Record (MoR).

- **למה MoR:** הוא הסולק הרשמי, מטפל ב-VAT/מס מכירה גלובלי במקומנו (חוקי מהיום
  הראשון), ומאפשר מוכר ישראלי עם payout (PayPal נתמך לישראל). מסך ההרשמה שלו
  מאפשר לבחור Israel — אומת.
- **עמלה:** ~5% + $0.50 לעסקה. במחיר של $4 העמלה הקבועה כבדה (~17%). זו
  ההוצאה האמיתית פר-מכירה (לא ה-LLM). המענה: חבילות קרדיטים (§3א) שפורסות את
  ה-$0.50 על פני עד 5 משרות.
- **גיבוי:** Paddle (גם MoR), אך תהליך אישור איטי ודוחה — לא מתאים לעליית-אוויר
  מהירה. להשאיר כתוכנית ב' בלבד.

### מה זה נוגע בו בקוד (מיושם)

- `src/lib/lemonsqueezy.ts` (מחליף את `stripe.ts`) — `lsConfigured`, `devFreeMode`,
  `createLsCheckout` (POST ל-`/v1/checkouts`, מיפוי tier→variant), ו-`verifyLsWebhook`
  (HMAC-SHA256 על ה-raw body, כותרת `X-Signature`).
- `src/app/api/payments/checkout/route.ts` + `.../webhook/route.ts` (במקום
  `api/stripe/*`). ה-`proxy.ts` matcher מחריג את `api/payments/webhook`.
- מנגנון ה-DB זהה: `purchases` נוצר `pending`, וה-webhook (`order_created` עם
  `status:"paid"`) מעדכן ל-`paid`. המטא-דאטה `user_id / job_id / tier` נוסעת
  ב-`checkout_data.custom` וחוזרת ב-`meta.custom_data` (`user_id / job_id`, או
  `user_id / order_id / sku` לחבילה — אין `tier`, יש מוצר אחד). עמודת
  `stripe_session_id` שונתה ל-`provider_ref` (migration `0005_lemonsqueezy.sql`).
- `.env` — `LEMONSQUEEZY_API_KEY / STORE_ID / WEBHOOK_SECRET` +
  `LEMONSQUEEZY_VARIANT_FULL` (ו-`_X2..X5` כאופציונליים). `DEV_FREE_MODE` נשאר.
- ה-gate ללא שינוי: כל עוד לא "paid", המשתמש רואה רק את הדגימה המטושטשת.

### מצב חינמי זמני — `NEXT_PUBLIC_FREE_MODE`

עד שחנות ה-Lemon Squeezy תאושר אי אפשר לגבות, ולכן המוצר ניתן בחינם תחת דגל אחד
(`src/lib/free-mode.ts`). התמחור עצמו לא בוטל: הפיוואל נשאר במקומו ורק המחיר בו
מוחלף ב-"Free while in beta", ו-`api/payments/checkout` מעניק את שורת ה-`purchases`
(או את ה-`orders` של החבילה) בעצמו במקום לפנות ל-LS. כל שאר הגזרות נגזרות מאותה
שורה `paid`, ולכן אף אחת מהן לא שונתה — וגם מסלול התשלום האמיתי וה-webhook נשארו
חיים כפי שהם. כיבוי = שינוי הדגל ל-`false` + דיפלוי (הערך מוטמע ב-build).

שתי השלכות: (1) אין תקרת שימוש — אנלוק חינמי מתנהג כמו רכישה ולכן פטור מהתקרה
היומית ב-`free-quota.ts`, כלומר חשיפה להוצאת LLM בלתי מוגבלת; (2) ההענקות
נשארות — כיבוי הדגל לא שולל גישה שכבר ניתנה, והשורות האלה יופיעו כאפס בכל דוח
הכנסות.

---

## 6. הגנה מפני ניצול החינמי (Anti-abuse)

"90% הגנה ב-10% מאמץ", בלי לפגוע במשתמשים אמיתיים:

- אימות אימייל חובה בהרשמה (קיים דרך OAuth/Supabase).
- חסימת דומייני מייל חד-פעמי (temp-mail / mailinator).
- Device/browser fingerprint להגבלת דגימות חינם לפי מכשיר.
- להימנע מחסמים כבדים (SMS / כרטיס אשראי לחינמי) — מורידים המרה יותר מהתועלת.

הערה מהקוד: היום הדגימה החינמית היא **אחת per `job`**, וכל `job` חדש שורף קריאת
LLM ללא תשלום (`freeSampleAvailable = !purchase && !generation`). אם העלות/ניצול
הופכים לבעיה — לקבוע תקרת jobs-per-day או samples-per-account.

---

## 7. מנועי צמיחה (לבנות לתוך המוצר)

- **ווטרמארק = הפצה:** כל CV שיוצא נושא את המותג. שיווק חינם.
- **איסוף מיילים:** רשימת מחפשי עבודה = הנכס למונטיזציה עתידית.
- **שיתוף (אופציונלי):** "שתף וקבל הטבה" להפיכת משתמשים למפיצים.

---

## 8. מחוץ ל-scope בשלב זה (עתידי)

- מנויים חודשיים (Basic/Pro) — רק אחרי שרואים משתמשים חוזרים. מנוי = החלטה =
  חיכוך = פחות טראפיק.
- תוספות שדרוג (ביקורת מומחה אנושי, אופטימיזציית LinkedIn).

(חבילות/קרדיטים מרובים היו כאן עד 2026-07-31 — הן מיושמות, ראו §3א.)

---

## 9. החלטות פתוחות

1. ~~**Tier `base` ($2)** — נשאר או מוסתר?~~ **הוכרע: נמחק** (2026-08-02),
   יחד עם `match`. יש מוצר אחד, `full` ב-$4 — ראה §3.
2. **מחיר פסיכולוגי** — $4 עגול מול $3.99. לעסקאות אימפולסיביות קטנות העגול
   נקי ומספיק.
3. **payout בפועל** — לאמת בזמן ההרשמה ל-Lemon Squeezy שישראל מקבלת PayPal payout.
4. **מיתוג** — לנעול שם אחד (SpeCV) ולעדכן app / OAuth / README / checkout.
